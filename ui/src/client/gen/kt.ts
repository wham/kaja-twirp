import { RpcTransport, ServiceInfo } from "@protobuf-ts/runtime-rpc";
export const model = {
  gens: [],
};
export function getClient(name: string, transport: RpcTransport): ServiceInfo | undefined {
  return undefined;
}
