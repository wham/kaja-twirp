#!/bin/sh
set -e
echo "Tidying node_modules"
cd "$(dirname "$0")/../app/node_modules"

rm -rf monaco-editor/dev
rm -rf monaco-editor/min
rm -rf monaco-editor/min-maps
find . -type d -path '*/typescript/lib/*' -exec rm -rf {} +

# du -h -d 1 app/node_modules | sort -hr